# Open some files with a default tool
alias -s tex=$EDITOR
alias -s php=$EDITOR
alias -s java=$EDITOR
alias -s pl=$EDITOR
alias -s py=$EDITOR

# Suffix aliases
alias -s pdf=evince
alias -s ps=evince
alias -s {flac,wav,wmv,mpg,avi,mpeg,ogm,m4v,mp4,mkv,ogg}='mpv'
alias -s {jpg,jpeg,xpm,xbm,png,gif}='eog'
